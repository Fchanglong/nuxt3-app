   const state = () => ({
        // 網站模塊狀態
    })
   const mutations = {
        // 網站模塊變更
    }
    const actions = {
        // 網站模塊行為
    }
    const getters = {
        // 網站模塊獲取器
    }

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}